#ifndef EX04_REPLACE_HPP
#define EX04_REPLACE_HPP

#include <iostream>
#include <fstream>
#include <string>
#include <cstdlib>

std::string replace(const std::string&	mutableStr, const std::string& substituteStr, std::string strInput);
void ft_error(const std::string& errorStr);

#endif
